# CRITICAL BUG FIX: Fix Part 2 UI Display (Missing 'Expected Effect' & Header Format)

The user reports that the **Part 2 (Detailed Plan)** UI is NOT showing the "Expected Effect" field, and the header format is incorrect.
We need to update `main.py` (UI rendering) and `utils.py` (Data generation) immediately.

# 1. Update `utils.py` - Enforce Data Fields
- Modify the `part2_detailed_plan` prompt.
- **Strict Output Schema**: The JSON object for each program MUST include:
  - `sub_category` (e.g., "생활")
  - `program_name` (e.g., "위생관리 지도")
  - `expected_effect` (e.g., "아동의 개인위생 습관 형성...") -> **CRITICAL: DO NOT MISS THIS.**
  - `target`, `count`, `frequency`, `content`.

# 2. Update `main.py` - Refine Rendering Logic
- Locate the `render_part2` function (or the loop displaying Part 2 data).
- **Change 1: Header Format with ">"**
  - Instead of just printing the program name, format the Markdown header as:
    `st.markdown(f"#### 📄 {item.get('sub_category', '영역')} > {item.get('program_name', '프로그램명')}")`
  - This satisfies the user's request for "Category > Program".

- **Change 2: Explicit "Expected Effect" Section**
  - Immediately after the header, check for `expected_effect`.
  - Render it prominently:
    ```python
    exp_effect = item.get('expected_effect', '기대효과 내용이 없습니다.')
    st.info(f"**🎯 기대효과**: {exp_effect}")
    ```
    *(Using `st.info` or colored text makes it stand out)*.

- **Change 3: Details & Content**
  - Follow with the existing details:
    `st.write(f"**대상**: {item['target']} | **인원**: {item['count']} | **주기**: {item['frequency']}")`
  - Then the main content:
    `st.markdown(item['content'])`

# 3. Add "Regenerate" Button
- Add a button `if st.button("🔄 Part 2 데이터 다시 생성 (수정사항 반영)"):`
- This calls the updated `utils.py` logic to ensure the new `expected_effect` field is actually generated by the AI for existing data.

# Action
Apply these UI and Logic fixes immediately. The user MUST see "Category > Program" and the "Expected Effect" box.